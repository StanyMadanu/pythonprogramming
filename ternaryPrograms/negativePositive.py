# a program to find negative, positive and zero values based on given input by using Ternary operator

num = int(input("Enter the number:"))

res = "Zero" if num==0 else "Positive number" if n  else "Negative number"

print("Given number is : {}".format(res))
